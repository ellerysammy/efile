import { LinkeararchivoPipe } from './linkeararchivo.pipe';

describe('LinkeararchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkeararchivoPipe();
    expect(pipe).toBeTruthy();
  });
});
